run yarn install in /cliend folder
run yarn install in /server folder

if bcrypt problem in /server
run :
npm uninstall bcrypt
npm audit fix --force
npm i bcrypt


to start /server run:
npx nodemon
to start /client run:
npm start
